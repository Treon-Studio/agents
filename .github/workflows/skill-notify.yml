name: Skill Added Notification

on:
  push:
    branches:
      - main
    paths:
      - '.opencode/skills/**'

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Detect new skills
        id: skills
        run: |
          # Get list of skills that were modified
          SKILLS_CHANGED=$(git diff --name-only HEAD~1 HEAD -- '.opencode/skills/**' 2>/dev/null | grep -v '\.gitignore' || true)

          if [ -z "$SKILLS_CHANGED" ]; then
            echo "No skills changed"
            echo "skills_json=[]" >> $GITHUB_OUTPUT
            echo "has_skills=false" >> $GITHUB_OUTPUT
          else
            echo "Skills changed:"
            echo "$SKILLS_CHANGED"

            # Build JSON array of skill names
            SKILLS_JSON="["
            FIRST=true
            for skill in $SKILLS_CHANGED; do
              SKILL_NAME=$(echo "$skill" | sed 's|.opencode/skills/||' | sed 's|/SKILL.md||' | sed 's|/.*||')
              if [ "$FIRST" = true ]; then
                FIRST=false
              else
                SKILLS_JSON="$SKILLS_JSON,"
              fi
              SKILLS_JSON="$SKILLS_JSON\"$SKILL_NAME\""
            done
            SKILLS_JSON="$SKILLS_JSON]"
            echo "skills_json=$SKILLS_JSON" >> $GITHUB_OUTPUT
            echo "has_skills=true" >> $GITHUB_OUTPUT

            echo "Detected skill changes"
          fi

      - name: Get commit info
        if: steps.skills.outputs.has_skills == 'true'
        run: |
          echo "Commit message: $(git log -1 --format='%s')"
          echo "Author: $(git log -1 --format='%an')"
          echo "SHA: ${{ github.sha }}"
          echo "Full SHA: $(git rev-parse HEAD)"

      - name: Send Discord notification
        if: steps.skills.outputs.has_skills == 'true'
        env:
          DISCORD_WEBHOOK: https://discord.com/api/webhooks/1502909275562053692/luKXCHFSA5F6Ifv4kCkSlsF4iXlbmOtdubVffvKj90ZJeebVl78oPjimY38p3H3R-u9m
          SKILLS_JSON: ${{ steps.skills.outputs.skills_json }}
          REPO: ${{ github.repository }}
          COMMIT_SHA: ${{ github.sha }}
        run: |
          echo "Building Discord notification..."

          # Get commit info from git (more reliable than GitHub context)
          AUTHOR_NAME=$(git log -1 --format='%an')
          COMMIT_MSG=$(git log -1 --format='%s')
          TIMESTAMP=$(git log -1 --format='%cI')
          COMMIT_URL="https://github.com/$REPO/commit/$COMMIT_SHA"

          echo "Commit info - Author: $AUTHOR_NAME, Message: $COMMIT_MSG"

          # Escape special characters for JSON
          ESCAPED_MSG=$(echo "$COMMIT_MSG" | sed 's/\\/\\\\/g' | sed 's/"/\\"/g' | sed ':a;N;$!ba;s/\n/\\n/g')
          ESCAPED_AUTHOR=$(echo "$AUTHOR_NAME" | sed 's/\\/\\\\/g' | sed 's/"/\\"/g')
          ESCAPED_REPO=$(echo "$REPO" | sed 's/\\/\\\\/g' | sed 's/"/\\"/g')

          # Create JSON payload file
          cat > payload.json << 'JSONEOF'
          {
            "embeds": [{
              "title": "🆕 New Skill Added",
              "color": 3447003,
              "description": "A new skill has been added to the repository",
              "fields": [
                {"name": "Repository", "value": "REPO_VAL", "inline": true},
                {"name": "Author", "value": "AUTHOR_VAL", "inline": true},
                {"name": "Skill(s) Added", "value": "SKILLS_VAL"},
                {"name": "Commit Message", "value": "MSG_VAL"}
              ],
              "url": "https://github.com/REPO_VAL/commit/SHA_VAL",
              "footer": {"text": "OpenAgents CI"},
              "timestamp": "TS_VAL"
            }]
          }
          JSONEOF

          # Replace placeholders
          sed -i "s/REPO_VAL/$ESCAPED_REPO/g" payload.json
          sed -i "s/AUTHOR_VAL/$ESCAPED_AUTHOR/g" payload.json
          sed -i "s/SKILLS_VAL/$SKILLS_JSON/g" payload.json
          sed -i "s/MSG_VAL/$ESCAPED_MSG/g" payload.json
          sed -i "s/SHA_VAL/$COMMIT_SHA/g" payload.json

          echo "Sending notification..."
          response=$(curl -s -w "%{http_code}" -o /dev/null -X POST \
            -H "Content-Type: application/json" \
            -d @payload.json \
            "$DISCORD_WEBHOOK")

          rm payload.json

          if [ "$response" = "204" ] || [ "$response" = "200" ]; then
            echo "✓ Discord notification sent successfully"
          else
            echo "✗ Failed to send Discord notification (HTTP $response)"
            exit 1
          fi